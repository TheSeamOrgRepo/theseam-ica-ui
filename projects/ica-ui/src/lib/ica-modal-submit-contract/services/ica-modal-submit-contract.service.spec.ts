/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing'
import { IcaModalSubmitContractService } from './ica-modal-submit-contract.service'

describe('Service: IcaModalSubmitContract', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [IcaModalSubmitContractService]
    })
  })

  it('should ...', inject([IcaModalSubmitContractService], (service: IcaModalSubmitContractService) => {
    expect(service).toBeTruthy()
  }))
})
